{
	"name": "server",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "rsbuild dev --env-dir \"../..\"",
		"build": "rsbuild build --env-dir \"../..\"",
		"watch": "wait-on http://localhost:8080 dist/entry.dev.js && dotenvx run -f \"../../.env.development\" -- bun --watch dist/entry.dev.js",
		"start": "dotenvx run -f \"../../.env.production\" -- bun dist/entry.prod.js"
	}
}
